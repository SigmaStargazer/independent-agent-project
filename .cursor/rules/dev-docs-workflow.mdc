---
description: 版本开发须先读 DevDocs 需求，生成 PRD/方案并等用户确认后再写代码
alwaysApply: true
---

# DevDocs 开发流程

版本化需求与方案统一放在仓库 **`DevDocs/`**（见 `DevDocs/README.md`）。

## 何时走本流程

以下情况**必须**先走文档流程：

- 用户提到 `DevDocs`、某版本号（如 `v0.2`），或「按需求 / PRD / 方案开发」
- 新功能、跨 Python/Unity/协议的中等及以上改动
- 版本目录下已有 `requirements/` 且任务与该版本相关

以下可**跳过**（除非用户要求走文档）：

- 明确的小 bug 修复、 typo、用户说「直接改 / 跳过文档」

## Agent 步骤

1. **定位版本目录**：用户指定 `DevDocs/vX.Y/`，或未指定则询问/列出已有版本。
2. **阅读** `requirements/` 下全部需求；必要时结合 `AGENTS.md`、`Doc/` 技术文档。
3. **生成或更新** 同目录下的 `PRD.md`、`solution.md`（UTF-8；可参考 `DevDocs/_template/`）。
4. **停止并等待确认**：告知用户审阅路径；**未得到明确确认前不得改业务代码**。
5. **确认后开发**：用户说「可以开发 / 已确认 / 按方案实现」再动手；完成后可在 `solution.md`「实现记录」补一行。

## 边界

- **不修改** `requirements/`（用户自有需求）。
- PRD/方案只写在对应版本文件夹内，不散落到 `Doc/` 除非用户要求归档。
