# 需求：MsgBox 增加 Btn3

> **来源**：用户 2026-08-19 口述需求（对话）  
> **关联**：v0.22.23 MsgBox 弹窗 Prefab 化（通用 `UIMsgBox` 当前支持 1~2 个按钮）

## 需求原文

v0.22.25需求：多加一个Btn3

## 说明

- 在 v0.22.23 的通用 MsgBox（`UIMsgBox` Prefab + `UIMsgBox.cs`）基础上，把支持的按钮数量从 **1~2** 扩展到 **1~3**。
- 触发场景 / 具体文案 / 按钮用途未指定，由实现按通用扩展处理（现有实例不受影响）。
